---
description: Cancel a running Cursor handoff job
argument-hint: <id>
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/scripts/cancel.sh:*)
---

Stop a running Cursor job and mark it cancelled.

```
${CLAUDE_PLUGIN_ROOT}/scripts/cancel.sh $ARGUMENTS
```
